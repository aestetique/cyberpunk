{{!--
    Standalone fumble card — used by actor.rollFumble() when there is no
    triggering chat card to embed into (macros, console). The card chrome
    wraps the shared fumble-section partial; embedded fumbles use the
    partial directly inside their parent card.
--}}
<div class="cyberpunk-card cyberpunk-card--fumble">
    {{> (sysPath "chat/partials/fumble-section.hbs") fumble=fumble}}
</div>
