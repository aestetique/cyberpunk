<div class="cyberpunk-card cyberpunk-card--attack">
    {{!-- Section Bar: Execute type --}}
    {{> "systems/cp2020/templates/chat/partials/section-bar.hbs" icon="strike" label=effectLabel}}

    {{!-- Weapon Line --}}
    {{> "systems/cp2020/templates/chat/partials/weapon-line.hbs"}}

    {{!-- Section Bar: Effect --}}
    {{> "systems/cp2020/templates/chat/partials/section-bar.hbs" icon="damage" label=(CPLocal "Effect")}}

    {{!-- Effect Row (shows the condition icon + label) --}}
    <div class="effect-row">
        <img src="systems/cp2020/img/conditions/{{effectIcon}}.svg" alt="">
        <span class="effect-row__label">{{effectLabel}}</span>
    </div>

    {{!-- Target Selector (for applying the condition) --}}
    <div class="target-selector" data-damage="{}" data-weapon-effect="{{conditionId}}" data-hit-location="">
        <div class="target-selector__tabs">
            <button class="target-selector__tab target-selector__tab--active" data-mode="targeted">{{CPLocal "Targeted"}}</button>
            <span class="target-selector__divider"></span>
            <button class="target-selector__tab" data-mode="selected">{{CPLocal "Selected"}}</button>
        </div>
        <div class="target-selector__content">
            <div class="target-info target-info--empty">
                {{CPLocal "SelectTargetEffect"}}
            </div>
        </div>
        <button class="apply-damage-btn" disabled>{{CPLocal "Apply"}}</button>
    </div>
</div>
