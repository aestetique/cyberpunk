<div class="ranged-attack-wrapper">
  <header class="reload-header">
    <span class="reload-title">{{weaponName}}</span>
    <a class="header-control close" data-action="closeDialog"><i class="fas fa-times"></i></a>
  </header>

  {{#if showFullAuto}}
    <button type="button" class="fire-mode-btn" data-action="pickFireMode" data-mode="fullAuto">
      {{localize "CYBERPUNK.FullAuto"}}
    </button>
  {{/if}}

  {{#if showBurst}}
    <button type="button" class="fire-mode-btn" data-action="pickFireMode" data-mode="threeRoundBurst">
      {{localize "CYBERPUNK.ThreeRoundBurst"}}
    </button>
  {{/if}}

  {{#if showTwoRoundBurst}}
    <button type="button" class="fire-mode-btn" data-action="pickFireMode" data-mode="twoRoundBurst">
      {{localize "CYBERPUNK.TwoRoundBurst"}}
    </button>
  {{/if}}

  {{#if showSingleShot}}
    <button type="button" class="fire-mode-btn" data-action="pickFireMode" data-mode="singleShot">
      {{localize "CYBERPUNK.SingleShot"}}
    </button>
  {{else}}
    <div class="reload-empty">{{localize "CYBERPUNK.OutOfAmmo"}}</div>
  {{/if}}

  {{#if showReload}}
    <button type="button" class="fire-mode-btn reload-btn" data-action="reload">
      {{localize "CYBERPUNK.Reload"}}
    </button>
  {{/if}}
</div>
