{{!--
  Reusable bonus list UI for drug Effect / Withdrawal tabs.
  Caller passes:
    bonusList         — array of shaped bonus rows
    attributeOptions  — dropdown options for the "Add Attribute" picker (stats.*)
    propertyOptions   — dropdown options for the "Add Property" picker (everything else)
    bonusSet          — "bonuses" | "withdrawal" — routes handler writes
    isLocked          — sheet lock state

  Mirrors the shared `parts/shared/tab-effect.hbs` structure (used by
  cyberware / outfit) — both render the same property/attribute split + op
  picker. Drug has its own copy because it manages two parallel bonus sets
  on the same sheet (Effect AND Withdrawal); the `bonusSet` data attribute
  routes each handler's writes to the right slot.
--}}
<div class="effect-bonuses" data-drop-target="skill-bonus" data-bonus-set="{{bonusSet}}">
  {{#each bonusList}}
    <div class="bonus-row" data-index="{{@index}}">

      {{#if this.isAttribute}}
        {{!-- Attribute bonus row (stats.*) --}}
        <label class="variable-label">{{localize "CYBERPUNK.Attribute"}}</label>
        {{#if ../isLocked}}
          <div class="dropdown">
            <span class="dropdown-text">{{this.label}}</span>
            <div class="dropdown-chevron">
              <img src="systems/cyberpunk/img/ui/icon-down.svg" alt="">
            </div>
          </div>
        {{else}}
          <div class="dropdown-wrapper">
            <select class="dropdown-select bonus-property-select" data-index="{{@index}}">
              <option value="{{this.property}}" selected>{{this.label}}</option>
              {{#each ../attributeOptions}}
                <option value="{{this.value}}">{{this.label}}</option>
              {{/each}}
            </select>
            <div class="dropdown-chevron">
              <img src="systems/cyberpunk/img/ui/icon-down.svg" alt="">
            </div>
          </div>
        {{/if}}
      {{else if this.isProperty}}
        {{!-- Property bonus row (non-stat) --}}
        <label class="variable-label">{{localize "CYBERPUNK.Property"}}</label>
        {{#if ../isLocked}}
          <div class="dropdown">
            <span class="dropdown-text">{{this.label}}</span>
            <div class="dropdown-chevron">
              <img src="systems/cyberpunk/img/ui/icon-down.svg" alt="">
            </div>
          </div>
        {{else}}
          <div class="dropdown-wrapper">
            <select class="dropdown-select bonus-property-select" data-index="{{@index}}">
              <option value="{{this.property}}" selected>{{this.label}}</option>
              {{#each ../propertyOptions}}
                <option value="{{this.value}}">{{this.label}}</option>
              {{/each}}
            </select>
            <div class="dropdown-chevron">
              <img src="systems/cyberpunk/img/ui/icon-down.svg" alt="">
            </div>
          </div>
        {{/if}}
      {{else}}
        {{!-- Skill bonus row --}}
        <label class="variable-label">{{localize "CYBERPUNK.Skill"}}</label>
        {{#if this.hasFilled}}
          <div class="skill-drop filled">
            <span class="skill-name" data-uuid="{{this.skillUuid}}">{{this.skillName}}</span>
          </div>
        {{else}}
          <div class="skill-drop empty" data-drop-target="skill-bonus">
            <span class="placeholder">{{#if ../isLocked}}{{localize "CYBERPUNK.DropSkillLocked"}}{{else}}{{localize "CYBERPUNK.DropSkillHere"}}{{/if}}</span>
          </div>
        {{/if}}
      {{/if}}

      {{!-- Operation: + / × / = (same width as the value input) --}}
      {{#if ../isLocked}}
        <span class="bonus-op">{{this.op}}</span>
      {{else}}
        <div class="dropdown-wrapper op-wrapper">
          <select class="dropdown-select bonus-op-select" data-index="{{@index}}">
            {{#each this.opOptions}}
              <option value="{{this.value}}" {{#if this.selected}}selected{{/if}}>{{this.label}}</option>
            {{/each}}
          </select>
          <div class="dropdown-chevron">
            <img src="systems/cyberpunk/img/ui/icon-down.svg" alt="">
          </div>
        </div>
      {{/if}}

      {{!-- Bonus value --}}
      {{#if ../isLocked}}
        <span class="bonus-value">{{this.value}}</span>
      {{else}}
        <input type="number" class="bonus-value-input" data-index="{{@index}}" value="{{this.value}}">
      {{/if}}

      {{!-- Trash icon --}}
      {{#unless ../isLocked}}
        <button type="button" class="remove-bonus" data-index="{{@index}}" title="Remove">
          <img src="systems/cyberpunk/img/ui/icon-trash.svg" alt="Remove">
        </button>
      {{/unless}}
    </div>
  {{/each}}
</div>

{{!-- Action buttons: Attribute → Property → Skill --}}
{{#unless isLocked}}
  <div class="effect-actions" data-bonus-set="{{bonusSet}}">
    <button type="button" class="add-attribute">{{localize "CYBERPUNK.AddAttribute"}}</button>
    <button type="button" class="add-property">{{localize "CYBERPUNK.AddProperty"}}</button>
    <button type="button" class="add-skill">{{localize "CYBERPUNK.AddSkill"}}</button>
  </div>
{{/unless}}
